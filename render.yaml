services:
  - type: web
    name: exchange-diary
    runtime: node
    plan: free
    autoDeploy: true
    buildCommand: npm install
    startCommand: npm run legacy:start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
    healthCheckPath: /api/health
